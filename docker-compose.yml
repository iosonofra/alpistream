services:
  mandrakodi-web:
    build: .
    container_name: mandrakodi-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
